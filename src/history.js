//u & i now incharge of history object, instead of
//React Router DOM creating it internally

//require("history").createBrowserHistory

import { createBrowserHistory } from 'history'; 
export default createBrowserHistory();
